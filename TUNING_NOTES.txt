SOLUSDBOT — Tuning for ~4–5 trades/day on SOL/USDT

Key changes:
- alpha.daily_trade_target = 5, cooloff_seconds = 600
- grid.k = 0.55 (narrower band), grid.levels = 16, notional_per_side_usdt = 250
- trading.reduce_only = false (otherwise entries are rejected)
- timeframe auto: favor 1m when under target; vol_hi_pct=1.2, vol_lo_pct=0.5
- pionex.symbol = SOLUSDT (no underscore)

Quick checklist if you still see 0 trades:
1) API key perms: enable trading, futures if using USDT perpetuals.
2) Symbol correctness on Pionex; adjust to exchange-required format if needed.
3) Min order size: check marketInfo; raise notional_per_side_usdt if per-level falls below exchange minimum.
4) Clock sync (UTC) and network; ensure Actions runner can reach Pionex.
5) Safe mode: if status shows WARN/PANIC often, grid widens; consider raising vol thresholds.
6) Confirm 'reduce_only' is false for entries; use true only for exits/hedges.
